import type { MockBlog } from "../lib/mockBlogs";
export const BLOGS_PROD: MockBlog[] = [
  // For now you can paste the same 15 items you put into db.json.
  // (Or keep using your existing MOCK_BLOGS if you prefer—see note below.)
];
